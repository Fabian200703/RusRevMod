RSA_CIVIL_WAR = {
	RSA_capture_cities = {

		priority = 10

		icon = generic_ignite_civil_war
		fire_only_once = yes

		visible = {

		}
	
		available = {
			has_war = yes
			AND = {
				188 = { is_fully_controlled_by = RSA }
				10 = { is_fully_controlled_by = RSA }
			}
		}
		cancel_trigger = {
			has_war = no
		}
		icon = mission_icon     # For GFX_decision_mission_icon
		is_good = yes
		days_mission_timeout = 85

		selectable_mission = yes
		complete_effect = {
			add_stability = 0.10
		}
		timeout_effect = {
			country_event = rusrev.1
		}
	}

	RSA_capture_cities_2 = {

		priority = 9

		icon = generic_ignite_civil_war
		fire_only_once = yes
		activation = { has_idea = February_Revolution}
		visible = {

		}
	
		available = {
			has_war = yes
			AND = {
				188 = { is_fully_controlled_by = RSA }
				10 = { is_fully_controlled_by = RSA }
			}
		}
		
		cancel_trigger = {
			has_war = no
		}
		icon = mission_icon     # For GFX_decision_mission_icon
		is_good = yes
		days_mission_timeout = 225

		selectable_mission = yes
		complete_effect = {
			add_stability = 0.10
		}
		timeout_effect = {
			country_event = RSA_communist_civil_war.001
		}
	}


	RSA_Collaps = {

		priority = 8

		icon = generic_ignite_civil_war
		fire_only_once = yes
		activation = { has_idea = Fallen_government}
		visible = {

		}
	
		available = {
			has_war = yes
			AND = {
				188 = { is_fully_controlled_by = RSA }
				10 = { is_fully_controlled_by = RSA }
			}
		}
		
		cancel_trigger = {
			has_war = no
		}
		icon = mission_icon     # For GFX_decision_mission_icon
		is_good = yes
		days_mission_timeout = 20

		selectable_mission = yes
		complete_effect = {
			add_stability = 0.10
		}
		timeout_effect = {
			country_event = CRA.001
			country_event = KCK.001
			country_event = SRA.001
			country_event = CRA.001
			country_event = RSA_anarchist_civil_war.001
			country_event = TCR.001
			country_event = SAN.001
			country_event = RCO.001
			country_event = DKO.001
		}
	}





























	
}